# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
#command=/usr/share/i3blocks/$BLOCK_NAME
separator_block_width=15
#markup=Hack Nerd Font Mono Regular 20
markup=Hack Liberation Mono  20


# Memory usage
# The type defaults to "mem" if the instance is not specified.
#[memory]
#label=MEM
#separator=false
#interval=30

# Network interface monitoring
#[iface]
#color=#00FF00
##border=#52aeff
#interval=10
#separator=false
#label= 

[RAM]
command=/home/noroot/.config/i3blocks/free.sh
interval=10
color=#10ffac
label=


[vpn]
command=/home/noroot/.config/i3blocks/vpn_status.sh
interval=10
color=#f46fff

[ip-local]
command=/home/noroot/.config/i3blocks/ethernet_status.sh
interval=10
color=#10ffac


